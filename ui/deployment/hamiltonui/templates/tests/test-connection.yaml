apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hamiltonui.fullname" . }}-test-connection"
  labels:
    {{- include "hamiltonui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hamiltonui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
