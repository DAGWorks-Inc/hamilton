apiVersion: v1
kind: Service
metadata:
  name: {{ include "hamiltonui.fullname" . }}
  labels:
    {{- include "hamiltonui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hamiltonui.selectorLabels" . | nindent 4 }}
